const EVENT_CATEGORIES = [ 'workshop', 'seminar', 'conference', 'training', 'meeting', 'social', 'other' ];

const RESOURCE_TYPES = [ 'room', 'equipment', 'vehicle', 'facility', 'technology', 'other' ];

const EVENT_STATUSES = [ 'upcoming', 'ongoing', 'completed', 'cancelled' ];

const BOOKING_STATUSES = [ 'confirmed', 'pending', 'cancelled' ];

const WAITING_LIST_STATUSES = [ 'waiting', 'notified', 'converted' ];

const USER_ROLES = [ 'admin', 'user' ];

module.exports = { EVENT_CATEGORIES, RESOURCE_TYPES, EVENT_STATUSES, BOOKING_STATUSES, WAITING_LIST_STATUSES, USER_ROLES };